{
  "name": "PeerTunes",
  "version": "0.0.1",
  "description": "PeerTunes",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify app.js -d -o public/js/app.js"
  },
  "dependencies": {
    "bittorrent-tracker": "^8.0.11",
    "ejs": "^2.4.2",
    "express": "^4.13.4",
    "hat": "0.0.3",
    "simple-peer": "^6.0.7"
  }
}
